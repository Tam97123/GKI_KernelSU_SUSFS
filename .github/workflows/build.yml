name: Kernel Build Process
permissions:
  contents: write
  actions: write

on:
  workflow_call:
    inputs:
      android_version:
        required: true
        type: string
      kernel_version:
        required: true
        type: string
      kernel_uname:
        required: false
        type: string
      sub_level:
        required: true
        type: string
      os_patch_level:
        required: true
        type: string
      kernel_variant:
        required: true
        type: string
      use_kpm:        
        required: false
        type: string
      use_zram:
        required: false
        type: string

jobs:
  build-gki:
    name: "${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.android_version }}-${{ inputs.os_patch_level }}"
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Maximize Build Space
        run: |
          curl -Ls https://raw.githubusercontent.com/apache/arrow/main/ci/scripts/util_free_space.sh | bash

      - name: Set Environment Variables
        run: |
          CONFIG="${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.sub_level }}"
          KERNEL_ROOT="$GITHUB_WORKSPACE/$CONFIG"

          cat >> $GITHUB_ENV << EOF
          DEFCONFIG=$KERNEL_ROOT/common/arch/arm64/configs/gki_defconfig
          CONFIG=$CONFIG
          KERNEL_ROOT=$KERNEL_ROOT
          SUSFS4KSU=$GITHUB_WORKSPACE/susfs4ksu
          KERNEL_PATCHES=$GITHUB_WORKSPACE/kernel_patches
          SUKI_KERNEL_PATCHES=$GITHUB_WORKSPACE/SukiSU_patches
          ANYKERNEL3=$GITHUB_WORKSPACE/AnyKernel3
          AVBTOOL=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/bin/avbtool
          MKBOOTIMG=$GITHUB_WORKSPACE/mkbootimg/mkbootimg.py
          UNPACK_BOOTIMG=$GITHUB_WORKSPACE/mkbootimg/unpack_bootimg.py
          BOOT_SIGN_KEY_PATH=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/share/avb/testkey_rsa2048.pem
          REPO=$GITHUB_WORKSPACE/git-repo/repo
          EOF

          echo "PATH=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/bin:$PATH" >> $GITHUB_ENV
          mkdir -p "$KERNEL_ROOT"

      - name: Setup Build Environment
        run: |
          AOSP_MIRROR=https://android.googlesource.com
          BRANCH=main-kernel-2025
          git clone $AOSP_MIRROR/kernel/prebuilts/build-tools -b $BRANCH --depth 1 kernel-build-tools &
          git clone $AOSP_MIRROR/platform/system/tools/mkbootimg -b $BRANCH --depth 1 mkbootimg &
          wait

          echo "AVBTOOL=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/bin/avbtool" >> $GITHUB_ENV
          echo "MKBOOTIMG=$GITHUB_WORKSPACE/mkbootimg/mkbootimg.py" >> $GITHUB_ENV
          echo "UNPACK_BOOTIMG=$GITHUB_WORKSPACE/mkbootimg/unpack_bootimg.py" >> $GITHUB_ENV
          echo "BOOT_SIGN_KEY_PATH=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/share/avb/testkey_rsa2048.pem" >> $GITHUB_ENV
          echo "PATH=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/bin:$PATH" >> $GITHUB_ENV

          mkdir -p ./git-repo
          curl https://storage.googleapis.com/git-repo-downloads/repo > ./git-repo/repo
          chmod a+rx ./git-repo/repo
          echo "REPO=$GITHUB_WORKSPACE/./git-repo/repo" >> $GITHUB_ENV

      - name: Set boot sign key
        env:
          BOOT_SIGN_KEY: ${{ secrets.BOOT_SIGN_KEY }}
        run: |
          if [ ! -z "$BOOT_SIGN_KEY" ]; then
            echo "$BOOT_SIGN_KEY" > ./kernel-build-tools/linux-x86/share/avb/testkey_rsa2048.pem
          fi

      - name: Clone AnyKernel3 and Kernel Patches
        run: |
          ANYKERNEL_BRANCH="gki-2.0"

          git clone https://github.com/Tam97123/AnyKernel3.git -b "$ANYKERNEL_BRANCH" && rm -rf AnyKernel3/.git
          git clone https://github.com/Tam97123/kernel_patches.git
          git clone https://github.com/ShirkNeko/SukiSU_patch.git

      - name: Determine SUSFS version
        run: |
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b "gki-${{ inputs.android_version }}-${{ inputs.kernel_version }}"
          cd susfs4ksu

          TARGET="${{ inputs.android_version }}-${{ inputs.kernel_version }}"
          if [ "${{ inputs.kernel_variant }}" = "WILD" ]; then
            case "$TARGET" in
              android12-5.10) git checkout 8a76ba240d1f7315352b49d97d854a4b166e5b47" ;;
              android13-5.10) git checkout e5c9eabfc6dcfc677d11f46ce1d5ff786de60a92" ;;
              android13-5.15) git checkout babf6be195576f09aae79650d47abf6a76e8a21b" ;;
              android14-5.15) git checkout 19f339e804ba9aebd9c6d735b965e2b9396c73ae" ;;
              android14-6.1) git checkout a162e2469d0b472545e5e46457eee171c0975fb0" ;;
              android15-6.6) git checkout f450ec00bf592d080f59b01ff6f9242456c9a427" ;;
            esac
            SUSFS_VERSION=v1.5.12
          else
            COMMIT=$(git log --all --grep="Bump version to" --format="%H" | head -n1)
            LOGS=$(git log -1 --format=%s "$COMMIT")
            VERSION=$(echo "$LOGS" | grep -oE '[0-9]+(\.[0-9]+){2}')
            echo "Newest susfs version: v$VERSION"
            SUSFS_VERSION="v${VERSION}"
          fi

          echo "SUSFS_VERSION=$SUSFS_VERSION" >> $GITHUB_ENV
              
      - name: Initialize and Sync Kernel Source
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          FORMATTED_BRANCH="${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.os_patch_level }}"

          $REPO init --depth=1 -u https://android.googlesource.com/kernel/manifest \
            -b common-${FORMATTED_BRANCH} --no-clone-bundle

          REMOTE_BRANCH=$(git ls-remote https://android.googlesource.com/kernel/common ${FORMATTED_BRANCH})
          DEFAULT_MANIFEST_PATH=.repo/manifests/default.xml
          if grep -q deprecated <<< $REMOTE_BRANCH; then
            sed -i "s/\"${FORMATTED_BRANCH}\"/\"deprecated\/${FORMATTED_BRANCH}\"/g" $DEFAULT_MANIFEST_PATH
          fi

          $REPO --trace sync -c -j$(nproc --all) --no-tags --no-clone-bundle --fail-fast

      - name: Extract Sublevel and Set File Name
        run: |
          ACTUAL_SUBLEVEL="${{ inputs.sub_level }}"
          if [ -f "$KERNEL_ROOT/common/Makefile" ]; then
            EXTRACTED=$(grep '^SUBLEVEL = ' "$KERNEL_ROOT/common/Makefile" | awk '{print $3}')
            [ -n "$EXTRACTED" ] && ACTUAL_SUBLEVEL="$EXTRACTED"
          fi

          ARTIFACT_BASE="${{ inputs.kernel_variant }}-${{ inputs.kernel_version }}.$ACTUAL_SUBLEVEL-${{ inputs.android_version }}-${{ inputs.os_patch_level }}"
          echo "ACTUAL_SUBLEVEL=$ACTUAL_SUBLEVEL" >> $GITHUB_ENV
          echo "ARTIFACT_BASE=$ARTIFACT_BASE" >> $GITHUB_ENV
          echo "FILE_NAME=$ARTIFACT_BASE" >> $GITHUB_ENV

      - name: Apply glibc 2.38 Compatibility Fix
        run: |
          # Use ACTUAL_SUBLEVEL for LTS builds, otherwise use the input sub_level
          if [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.10" && $ACTUAL_SUBLEVEL -le 186 ]] || 
             [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.15" && $ACTUAL_SUBLEVEL -le 119 ]] || 
             [[ "${{ inputs.android_version }}" == "android14" && "${{ inputs.kernel_version }}" == "5.15" && $ACTUAL_SUBLEVEL -le 110 ]] || 
             [[ "${{ inputs.android_version }}" == "android14" && "${{ inputs.kernel_version }}" == "6.1" && $ACTUAL_SUBLEVEL -le 43 ]]; then
            GLIBC_VERSION=$(ldd --version 2>/dev/null | head -n 1 | awk '{print $NF}') 
            if [ "$(printf '%s\n' "2.38" "$GLIBC_VERSION" | sort -V | head -n1)" = "2.38" ]; then 
              cd "$KERNEL_ROOT/common"
              sed -i '/\$(Q)\$(MAKE) -C \$(SUBCMD_SRC) OUTPUT=\$(abspath \$(dir \$@))\/ \$(abspath \$@)/s//$(Q)$(MAKE) -C $(SUBCMD_SRC) EXTRA_CFLAGS="$(CFLAGS)" OUTPUT=$(abspath $(dir $@))\/ $(abspath $@)/' tools/bpf/resolve_btfids/Makefile 2>/dev/null || true
              if [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.10" && $ACTUAL_SUBLEVEL -le 186 ]] || 
                 [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.15" && $ACTUAL_SUBLEVEL -le 119 ]] || 
                 [[ "${{ inputs.android_version }}" == "android14" && "${{ inputs.kernel_version }}" == "5.15" && $ACTUAL_SUBLEVEL -le 110 ]]; then
                sed -i '/char \*buf = NULL;/a int i;' tools/lib/subcmd/parse-options.c 2>/dev/null || true
                sed -i 's/for (int i = 0; subcommands\[i\]; i++) {/for (i = 0; subcommands[i]; i++) {/' tools/lib/subcmd/parse-options.c 2>/dev/null || true
                sed -i '/if (subcommands) {/a int i;' tools/lib/subcmd/parse-options.c 2>/dev/null || true
                sed -i 's/for (int i = 0; subcommands\[i\]; i++)/for (i = 0; subcommands[i]; i++)/' tools/lib/subcmd/parse-options.c 2>/dev/null || true
              fi
            fi
          fi 

      - name: Fix Less Then 6.6.50 Builds
        if: inputs.android_version == 'android15' && inputs.kernel_version == '6.6'
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          if ! grep -qxF '#include <trace/hooks/fs.h>' ./fs/namespace.c; then
              sed -i '/#include <trace\/hooks\/blk.h>/a #include <trace\/hooks\/fs.h>' ./fs/namespace.c
          fi

      - name: Apply ptrace patch for older kernels
        if: fromJSON(inputs.kernel_version) < 5.16
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          patch -p1 -F 3 < "$KERNEL_PATCHES/gki_ptrace.patch"

      - name: Add KernelSU
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          case "${{ inputs.kernel_variant }}" in
            WILD)
              curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s wild
              ;;
            NEXT)
              curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -s next
              ;;
            KSU)
              curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s main
              ;;
            SUKI)
              curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s builtin
              ;;
          esac

      - name: Getting KernelSU Version
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          case "${{ inputs.kernel_variant }}" in
            WILD)
              # OLD scheme
              BASE_VERSION=10200
              COMMIT_COUNT=$(git -C "$KERNEL_ROOT/Wild_KSU" rev-list --count HEAD)
              KSU_VERSION=$(expr $BASE_VERSION + $COMMIT_COUNT)
              ;;
            NEXT)
              # NEW scheme
              BASE_VERSION=30000
              COMMIT_COUNT=$(git -C "$KERNEL_ROOT/KernelSU-Next" rev-list --count HEAD)
              KSU_VERSION=$(expr $BASE_VERSION + $COMMIT_COUNT)
              ;;
            KSU)
              # NEW scheme
              BASE_VERSION=30000
              COMMIT_COUNT=$(git -C "$KERNEL_ROOT/KernelSU" rev-list --count HEAD)
              KSU_VERSION=$(expr $BASE_VERSION + $COMMIT_COUNT)
              ;;
            SUKI)
              # NEW scheme
              BASE_VERSION=40000
              COMMIT_COUNT=$(git -C "$KERNEL_ROOT/KernelSU" rev-list --count HEAD)
              KSU_VERSION=$(expr $BASE_VERSION + $COMMIT_COUNT)
              ;;
          esac
          echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV

      - name: Apply SUSFS Patches for KernelSU Variants
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          cp "$SUSFS4KSU/kernel_patches/fs/"* "$KERNEL_ROOT/common/fs/"
          cp "$SUSFS4KSU/kernel_patches/include/linux/"* "$KERNEL_ROOT/common/include/linux/"

          # Apply core SUSFS patches
          cd "$KERNEL_ROOT/common"
          cp "$SUSFS4KSU/kernel_patches/50_add_susfs_in_gki-${{ inputs.android_version }}-${{ inputs.kernel_version }}.patch" ./
          patch -p1 < 50_add_susfs_in_gki-${{ inputs.android_version }}-${{ inputs.kernel_version }}.patch || true

          case "${{ inputs.kernel_variant }}" in
            WILD)
              cd "$KERNEL_ROOT/Wild_KSU"
              cp "$SUSFS4KSU/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch" .
              patch -p1 < 10_enable_susfs_for_ksu.patch || true
              cp "$KERNEL_PATCHES/wild/susfs_fix_patches/${SUSFS_VERSION}/fix_core_hook.c.patch" ./
              patch -p1 < fix_core_hook.c.patch
              cp "$KERNEL_PATCHES/wild/susfs_fix_patches/${SUSFS_VERSION}/fix_sucompat.c.patch" ./
              patch -p1 < fix_sucompat.c.patch
              cp "$KERNEL_PATCHES/wild/susfs_fix_patches/${SUSFS_VERSION}/fix_kernel_compat.c.patch" ./
              patch -p1 < fix_kernel_compat.c.patch
              ;;
            NEXT)
              cd "$KERNEL_ROOT/KernelSU-Next"
              cp "$SUSFS4KSU/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch" .
              patch -p1 < 10_enable_susfs_for_ksu.patch || true
              ;;
            KSU)
              cd "$KERNEL_ROOT/KernelSU"
              cp "$SUSFS4KSU/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch" .
              patch -p1 < 10_enable_susfs_for_ksu.patch || true
              ;;
          esac

          cd "$KERNEL_ROOT/common"
          # Post-SUSFS sed safeguards for task_mmu.c 'goto show_pad' handling
          if [[ "${{ inputs.android_version }}" == "android12" && "${{ inputs.kernel_version }}" == "5.10" && $ACTUAL_SUBLEVEL -le 209 ]] ||
             [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.10" && "${{ inputs.os_patch_level }}" != "2024-05" && $ACTUAL_SUBLEVEL -le 209 ]] ||
             [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.15" && "${{ inputs.os_patch_level }}" != "2024-05" && $ACTUAL_SUBLEVEL -le 148 ]] ||
             [[ "${{ inputs.android_version }}" == "android14" && "${{ inputs.kernel_version }}" == "5.15" && "${{ inputs.os_patch_level }}" != "2024-05" && $ACTUAL_SUBLEVEL -le 148 ]] ||
             [[ "${{ inputs.android_version }}" == "android14" && "${{ inputs.kernel_version }}" == "6.1" && "${{ inputs.os_patch_level }}" != "2024-05" && $ACTUAL_SUBLEVEL -le 75 ]]; then
            sed -i -e 's/goto show_pad;/return 0;/' ./fs/proc/task_mmu.c
          fi

          # Apply additional SUSFS patches for Android 13 5.15 compatibility
          if [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.15" ]] && [[ ${{ inputs.sub_level }} -le 41 ]]; then
            cp $KERNEL_PATCHES/ksu/susfs_fix_patches/v2.0.0/fix_fdinfo.c.patch ./
            patch -p1 < fix_fdinfo.c.patch || true
            cp $KERNEL_PATCHES/ksu/susfs_fix_patches/v2.0.0/fix_namespace.c.patch ./
            patch -p1 < fix_namespace.c.patch || true
            cp $KERNEL_PATCHES/ksu/susfs_fix_patches/v2.0.0/fix_open.c.patch ./
            patch -p1 < fix_open.c.patch || true
            sed -i '/#include <linux\/syscalls.h>/a #include <linux/susfs.h>' common/fs/open.c
            sed -i 's/i_uid_into_mnt(i_user_ns(inode), inode)/i_uid_into_mnt(inode->i_sb->s_user_ns, inode)/g' fs/susfs.c
          fi

          # Use ACTUAL_SUBLEVEL for LTS or actual kernel sublevel if available
          if [[ "${{ inputs.android_version }}" == "android12" && "${{ inputs.kernel_version }}" == "5.10" && $ACTUAL_SUBLEVEL -le 43 ]] ||
             [[ "${{ inputs.android_version }}" == "android14" && "${{ inputs.kernel_version }}" == "6.1" && $ACTUAL_SUBLEVEL -ge 145 ]] ||
             [[ "${{ inputs.android_version }}" == "android15" && "${{ inputs.kernel_version }}" == "6.6" && $ACTUAL_SUBLEVEL -ge 98 ]]; then
            cp $KERNEL_PATCHES/ksu/susfs_fix_patches/v2.0.0/1_fix_base.c.patch ./
            patch -p1 < 1_fix_base.c.patch || true
          fi

          if [[ "${{ inputs.android_version }}" == "android12" && "${{ inputs.kernel_version }}" == "5.10" && $ACTUAL_SUBLEVEL -le 43 ]]; then
            cp $KERNEL_PATCHES/ksu/susfs_fix_patches/v2.0.0/2_fix_base.c.patch ./
            patch -p1 < 2_fix_base.c.patch || true
          fi

          if [[ "${{ inputs.android_version }}" == "android15" && "${{ inputs.kernel_version }}" == "6.6" && $ACTUAL_SUBLEVEL -le 58 ]]; then
            cp $KERNEL_PATCHES/ksu/susfs_fix_patches/v2.0.0/fix_task_mmu.c.patch ./
            patch -p1 < fix_task_mmu.c.patch || true
          fi

      - name: Apply Hooks Patches
        if: inputs.kernel_variant == 'WILD'
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          cp "$KERNEL_PATCHES/wild/hooks/scope_min_manual_hooks_v1.4.patch" ./
          patch -p1 -F 3 < scope_min_manual_hooks_v1.4.patch

      - name: Add BBG
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          curl -LSs https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash
          echo "CONFIG_BBG=y" >> common/arch/arm64/configs/gki_defconfig
          sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/lockdown/lockdown,baseband_guard/ } }' common/security/Kconfig

      - name: Apply Module Check Bypass
        run: |
          if [[ "${{ inputs.kernel_version }}" == "6.1" || "${{ inputs.kernel_version }}" == "6.6" || "${{ inputs.kernel_version }}" == "6.12" ]]; then
            sed -i '/bad_version:/{:a;n;/return 0;/{s/return 0;/return 1;/;b};ba}' "$KERNEL_ROOT/common/kernel/module/version.c"
          else
            sed -i '/bad_version:/{:a;n;/return 0;/{s/return 0;/return 1;/;b};ba}' "$KERNEL_ROOT/common/kernel/module.c"
          fi

      - name: Apply LZ4KD & ZSTDN patches
        if: inputs.use_zram == 'true' && inputs.android_version != '6.12'
        run: |
            cd "$CONFIG/common"
            cp -r $SUKI_KERNEL_PATCHES/other/zram/lz4k/include/linux/* ./include/linux/
            cp -r $SUKI_KERNEL_PATCHES/other/zram/lz4k/lib/* ./lib/
            cp -r $SUKI_KERNEL_PATCHES/other/zram/lz4k/crypto/* ./crypto/
            cp -r $SUKI_KERNEL_PATCHES/other/zram/lz4k_oplus ./lib/

            cp $SUKI_KERNEL_PATCHES/other/zram/zram_patch/${{ inputs.kernel_version }}/lz4kd.patch ./
            echo "Applying lz4kd patch"
            patch -p1 -F 3 < lz4kd.patch || true

            cp $SUKI_KERNEL_PATCHES/other/zram/zram_patch/${{ inputs.kernel_version }}/lz4k_oplus.patch ./
            echo "Applying lz4k_oplus patch"
            patch -p1 -F 3 < lz4k_oplus.patch || true

      - name: Fix WiFi and Bluetooth on Samsung 6.6 GKI devices
        if: inputs.kernel_version == '6.6'
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          SYMBOL_LIST=android/abi_gki_aarch64_galaxy
          echo "kdp_set_cred_non_rcu" >> $SYMBOL_LIST
          echo "kdp_usecount_dec_and_test" >> $SYMBOL_LIST
          echo "kdp_usecount_inc" >> $SYMBOL_LIST

          PATCH="$KERNEL_PATCHES/samsung/min_kdp/add-min_kdp-symbols.patch"
          if patch -p1 --dry-run < "$PATCH"; then
            patch -p1 --no-backup-if-mismatch < $PATCH
          fi

          cp "$KERNEL_PATCHES/samsung/min_kdp/min_kdp.c" drivers/min_kdp.c
          echo "obj-y += min_kdp.o" >> drivers/Makefile

      - name: Fix WiFi and Bluetooth on Xiaomi 6.6 GKI devices
        if: ${{ ( inputs.kernel_version == '6.6' ) }}
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          SYMBOL_LIST=android/abi_gki_aarch64_xiaomi
          echo "device_find_any_child" >> $SYMBOL_LIST

      - name: Configure Kernel Options
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          # Suki ZRAM and KPM
          if [ "${{ inputs.kernel_variant }}" == "SUKI" ] && [ "${{ inputs.android_version }}" != "android16"  ]; then
            if [ "${{ inputs.use_zram }}" == "true" ]; then
              if [ "${{ inputs.kernel_version }}" = "5.10" ]; then
                echo "CONFIG_ZSMALLOC=y" >> "$DEFCONFIG"
                echo "CONFIG_ZRAM=y" >> "$DEFCONFIG"
                echo "CONFIG_MODULE_SIG=n" >> "$DEFCONFIG"
                echo "CONFIG_CRYPTO_LZO=y" >> "$DEFCONFIG"
                echo "CONFIG_ZRAM_DEF_COMP_LZ4KD=y" >> "$DEFCONFIG"
              fi

              if [ "${{ inputs.kernel_version }}" != "6.6" ] && [ "${{ inputs.kernel_version }}" != "5.10" ]; then
                if grep -q "CONFIG_ZSMALLOC" -- "$DEFCONFIG"; then
                    sed -i 's/CONFIG_ZSMALLOC=m/CONFIG_ZSMALLOC=y/g' "$DEFCONFIG"
                else
                    echo "CONFIG_ZSMALLOC=y" >> "$DEFCONFIG"
                fi
                sed -i 's/CONFIG_ZRAM=m/CONFIG_ZRAM=y/g' "$DEFCONFIG"
              fi

              if [ "${{ inputs.kernel_version }}" = "6.6" ]; then
                echo "CONFIG_ZSMALLOC=y" >> "$DEFCONFIG"
                sed -i 's/CONFIG_ZRAM=m/CONFIG_ZRAM=y/g' "$DEFCONFIG"
              fi

              if [ "${{ inputs.android_version }}" = "android14" ] || [ "${{ inputs.android_version }}" = "android15" ]; then
                sed -i 's/"drivers\/block\/zram\/zram\.ko",//g; s/"mm\/zsmalloc\.ko",//g' ./common/modules.bzl
                echo "CONFIG_MODULE_SIG_FORCE=n" >> "$DEFCONFIG"
              fi

              if grep -q "CONFIG_ZSMALLOC=y" "$DEFCONFIG" && grep -q "CONFIG_ZRAM=y" "$DEFCONFIG"; then
                echo "CONFIG_CRYPTO_LZ4HC=y" >> "$DEFCONFIG"
                echo "CONFIG_CRYPTO_LZ4K=y" >> "$DEFCONFIG"
                echo "CONFIG_CRYPTO_LZ4KD=y" >> "$DEFCONFIG"
                echo "CONFIG_CRYPTO_842=y" >> "$DEFCONFIG"
                echo "CONFIG_CRYPTO_LZ4K_OPLUS=y" >> "$DEFCONFIG"
                echo "CONFIG_ZRAM_WRITEBACK=y" >> "$DEFCONFIG"
              fi
            fi

            if [ "${{ inputs.use_kpm }}" == "true" ] && [ "${{ inputs.android_version }}" != "android15" ]; then
              echo "CONFIG_KPM=y" >> "$DEFCONFIG"
            fi
          fi

          cat >> "${{ env.DEFCONFIG }}" << 'EOF'
          # KernelSU Configuration
          CONFIG_KSU=y
          CONFIG_KSU_KPROBES_HOOK=n

          # Mountify Support
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y

          # Networking Configuration
          CONFIG_IP_NF_TARGET_TTL=y
          CONFIG_IP6_NF_TARGET_HL=y
          CONFIG_IP6_NF_MATCH_HL=y

          # BBR TCP Congestion Control
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_BBR=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_TCP_CONG_BIC=n
          CONFIG_TCP_CONG_WESTWOOD=n
          CONFIG_TCP_CONG_HTCP=n

          # IPSet Support
          CONFIG_IP_SET=y
          CONFIG_IP_SET_MAX=65534
          CONFIG_IP_SET_BITMAP_IP=y
          CONFIG_IP_SET_BITMAP_IPMAC=y
          CONFIG_IP_SET_BITMAP_PORT=y
          CONFIG_IP_SET_HASH_IP=y
          CONFIG_IP_SET_HASH_IPMARK=y
          CONFIG_IP_SET_HASH_IPPORT=y
          CONFIG_IP_SET_HASH_IPPORTIP=y
          CONFIG_IP_SET_HASH_IPPORTNET=y
          CONFIG_IP_SET_HASH_IPMAC=y
          CONFIG_IP_SET_HASH_MAC=y
          CONFIG_IP_SET_HASH_NETPORTNET=y
          CONFIG_IP_SET_HASH_NET=y
          CONFIG_IP_SET_HASH_NETNET=y
          CONFIG_IP_SET_HASH_NETPORT=y
          CONFIG_IP_SET_HASH_NETIFACE=y
          CONFIG_IP_SET_LIST_SET=y

          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_SUS_SU=n
          EOF

          # Remove check_defconfig
          if [[ "${{ inputs.android_version }}" == "android16" && "${{ inputs.kernel_version }}" == "6.12" ]]; then
            sed -i '/name = "kernel_aarch64",/a\    check_defconfig = "disabled",' common/BUILD.bazel
            awk '/name = "kernel_aarch64"/,/^\)/ {if(/check_defconfig = "disabled"/) found=1} END{if(found) print "SUCCESS: check_defconfig = \"disabled\" is in the kernel_aarch64 block"; else print "FAILED: NOT in the kernel_aarch64 block"}' common/BUILD.bazel
          else
            sed -i 's/check_defconfig//' ./common/build.config.gki
          fi

      - name: Change Kernel Name
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          #Wild to Kernel Version (replace last instance only)
          if [[ "${{ inputs.kernel_version }}" == "5."* ]] || [[ "${{ inputs.kernel_version }}" == "6.1" ]]; then
          if [ -z "${{ inputs.kernel_uname }}" ]; then
            echo "Didn't set kernel uname! Set to default value."
            sed -i "\$s|echo \"\$res\"|echo \"-${{ inputs.android_version }}-${KSUVER}-Tam\"|" ./common/scripts/setlocalversion
          else
            echo "Set kernel uname: ${{ inputs.kernel_uname }}!"
            sed -i "\$s|echo \"\$res\"|echo \"-${{ inputs.android_version }}-${{ inputs.kernel_uname }}\"|" ./common/scripts/setlocalversion
          fi
          else
            if [ -z "${{ inputs.kernel_uname }}" ]; then
              echo "Didn't set kernel uname! Set to default value."
              perl -i -0777 -pe 's/(.*)echo "\$\{KERNELVERSION\}\$\{file_localversion\}\$\{config_localversion\}\$\{LOCALVERSION\}\$\{scm_version\}"/$1echo "\${KERNELVERSION}\${KSUVER}-Tam"/s' ./common/scripts/setlocalversion
            else
              echo "Set kernel uname: ${{ inputs.kernel_uname }}!"
              perl -i -0777 -pe 's/(.*)echo "\$\{KERNELVERSION\}\$\{file_localversion\}\$\{config_localversion\}\$\{LOCALVERSION\}\$\{scm_version\}"/$1echo "\${KERNELVERSION}\${kernel_uname}"/s' ./common/scripts/setlocalversion
            fi
          fi

          if [ -f "build/build.sh" ]; then
            #Remove Dirty Flag
            sed -i 's/-dirty//' ./common/scripts/setlocalversion
          else
            #Remove Dirty Flag
            sed -i "/stable_scmversion_cmd/s/-maybe-dirty//g" ./build/kernel/kleaf/impl/stamp.bzl
            #Remove Abi Exports and Error
            rm -rf ./common/android/abi_gki_protected_exports_*
            perl -pi -e 's/^\s*"protected_exports_list"\s*:\s*"android\/abi_gki_protected_exports_aarch64",\s*$//;' ./common/BUILD.bazel
          fi

      - name: Build Kernel
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          set -ex
          if [ "${{ inputs.kernel_variant }}" == "SUKI" ] && [ "${{ inputs.android_version }}" == "android13" ] && [ "${{ inputs.kernel_version }}" == "5.15" ]; then
            LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh || true
            if [ ! -f "./out/android13-5.15/dist/Image" ]; then
              if [ ! -f "./out/android13-5.15/common/modules.order" ]; then
                exit 1
              else
                cp out/android13-5.15/common/modules.order common/android/gki_aarch64_modules
                echo "Building the kernel again..."
                LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh || exit 1
              fi
            fi
          elif [ -f "build/build.sh" ]; then
            LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh || exit 1
          else
            tools/bazel build --config=fast --config=stamp --lto=thin //common:kernel_aarch64_dist || exit 1
          fi

      - name: Patch KPM for SukiSU-Ultra
        if: inputs.kernel_variant == 'SUKI' && inputs.android_version != 'android15' && inputs.android_version != 'android16' && inputs.use_kpm == 'true'
        run: |
          echo "Patching Image file..."
          if [ -f "$KERNEL_ROOT/out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist/Image" ]; then
            cd "$KERNEL_ROOT/out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist"
          elif [ -f "$KERNEL_ROOT/bazel-bin/common/kernel_aarch64/Image" ]; then
            cd "$KERNEL_ROOT/bazel-bin/common/kernel_aarch64"
          else
            echo "Error: Image file not found in any expected location"
            exit 1
          fi
          curl -LSs "https://raw.githubusercontent.com/ShirkNeko/SukiSU_patch/refs/heads/main/kpm/patch_linux" -o patch
          chmod +x patch
          ./patch
          rm -f Image
          mv oImage Image

      - name: Prepare AnyKernel3
        run: |
          # Copy Image from normal build locations
          if [ -f "$KERNEL_ROOT/out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist/Image" ]; then
            cp "$KERNEL_ROOT/out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist/Image" "$ANYKERNEL3/Image"
          elif [ -f "$KERNEL_ROOT/bazel-bin/common/kernel_aarch64/Image" ]; then
            cp "$KERNEL_ROOT/bazel-bin/common/kernel_aarch64/Image" "$ANYKERNEL3/Image"
          else
            echo "Error: Image file not found in any expected location"
            exit 1
          fi

      - name: Create Bootimgs Folder and Copy Images
        run: |
          mkdir bootimgs

          # Determine source directory based on Android version
          if [[ "${{ inputs.android_version }}" == "android12" || "${{ inputs.android_version }}" == "android13" ]]; then
            SRC_DIR="$KERNEL_ROOT/out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist"
          else
            SRC_DIR="$KERNEL_ROOT/bazel-bin/common/kernel_aarch64"
          fi

          cp "$SRC_DIR/Image" ./bootimgs/
          cp "$SRC_DIR/Image.lz4" ./bootimgs/
          cp "$SRC_DIR/Image" "./${{ env.FILE_NAME }}-Image"
          cp "$SRC_DIR/Image.lz4" "./${{ env.FILE_NAME }}-Image.lz4"

          # Create gzip of the Image files
          gzip -n -k -f -9 "./${{ env.FILE_NAME }}-Image"
          gzip -n -k -f -9 ./bootimgs/Image

      - name: Build Boot Images
        working-directory: bootimgs
        run: |
          RAMDISK_ARGS=""

          # Android 12 needs ramdisk from GKI certified boot
          if [[ "${{ inputs.android_version }}" == "android12" ]]; then
            GKI_URL="https://dl.google.com/android/gki/gki-certified-boot-android12-5.10-${{ inputs.os_patch_level }}_r1.zip"
            FALLBACK_URL="https://dl.google.com/android/gki/gki-certified-boot-android12-5.10-2023-01_r1.zip"
            
            if curl -sL -w "%{http_code}" "$GKI_URL" -o /dev/null | grep -q 200; then
              curl -Lo gki-kernel.zip "$GKI_URL"
            else
              curl -Lo gki-kernel.zip "$FALLBACK_URL"
            fi
            
            unzip -q gki-kernel.zip && rm gki-kernel.zip
            $UNPACK_BOOTIMG --boot_img="$(pwd)/boot-5.10.img"
            RAMDISK_ARGS="--ramdisk out/ramdisk --os_version 12.0.0 --os_patch_level ${{ inputs.os_patch_level }}"
          fi

          # Build all three boot image variants
          for suffix in "" ".gz" ".lz4"; do
            kernel="Image${suffix}"
            output="boot${suffix//./-}.img"
            [ -z "$suffix" ] && output="boot.img"
            
            $MKBOOTIMG --header_version 4 --kernel "$kernel" --output "$output" $RAMDISK_ARGS
            $AVBTOOL add_hash_footer --partition_name boot --partition_size $((64 * 1024 * 1024)) \
              --image "$output" --algorithm SHA256_RSA2048 --key "$BOOT_SIGN_KEY_PATH"
            cp "./$output" "$GITHUB_WORKSPACE/${{ env.FILE_NAME }}-$output"
          done  
          
      - name: Scan and collect patch rejects
        if: ${{ always() }}
        run: |
          set -e
          CONFIG_DIR="$KERNEL_ROOT"
          REJECTS_DIR="$GITHUB_WORKSPACE/patch-rejects"
          mkdir -p "$REJECTS_DIR"
          # Find all .rej files under the configuration directory
          mapfile -t REJS < <(find "$CONFIG_DIR" -type f -name '*.rej')
          REJ_COUNT=${#REJS[@]}
          echo "Found $REJ_COUNT .rej files under $CONFIG_DIR"
          echo "REJ_COUNT=$REJ_COUNT" >> $GITHUB_ENV
          if [ "$REJ_COUNT" -gt 0 ]; then
            for REJ in "${REJS[@]}"; do
              # Relative path from $CONFIG
              REL="${REJ#"$CONFIG_DIR"/}"
              DEST="$REJECTS_DIR/$REL"
              mkdir -p "$(dirname "$DEST")"
              cp "$REJ" "$DEST"
              # Copy the associated original file alongside the .rej
              ORIG="${REJ%.rej}"
              if [ -f "$ORIG" ]; then
                ORIG_DEST="$REJECTS_DIR/${REL%.rej}"
                mkdir -p "$(dirname "$ORIG_DEST")"
                cp "$ORIG" "$ORIG_DEST"
              fi
              echo "$REL" >> "$REJECTS_DIR/index.txt"
            done
          fi
  
      - name: Generate Build Summary
        if: ${{ always() }}
        run: |
          set -e
  
          REJECTS_DIR="$GITHUB_WORKSPACE/patch-rejects"
          OUT="build_summary.md"
  
          {
            echo "# Patch Reject Summary"
            echo "## ${{ env.FILE_NAME }}"
            echo ""
            echo "- Rejects directory: \`$REJECTS_DIR\`"
            echo "- Kernel root: \`$KERNEL_ROOT\`"
            echo ""
          } > "$OUT"
  
          if [ ! -s "$REJECTS_DIR/index.txt" ]; then
            echo "No patch rejects found." >> "$OUT"
            # cat "$OUT" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
  
          sort -u "$REJECTS_DIR/index.txt" > "$REJECTS_DIR/index.sorted.txt"
  
          echo "## Reject Files" >> "$OUT"
          while IFS= read -r rel; do
            [ -n "$rel" ] && echo "- \`$rel\`" >> "$OUT"
          done < "$REJECTS_DIR/index.sorted.txt"
  
          echo "" >> "$OUT"
          echo "## Grouped By Directory (count)" >> "$OUT"
          awk -F/ 'NF{dir=$0; sub(/\/[^[\/]]*$/, "", dir); if(dir==$0) dir="."; c[dir]++} END{for (d in c) printf "%d\t%s\n", c[d], d}' "$REJECTS_DIR/index.sorted.txt" \
            | sort -nr \
            | while IFS=$'\t' read -r cnt dir; do
                [ -n "$dir" ] && echo "- **$cnt** \`$dir\`" >> "$OUT"
              done
          echo "" >> "$OUT"
          echo "## Grouped By Reject Content (sha256)" >> "$OUT"
  
          TMP="$(mktemp)"
          while IFS= read -r rel; do
            [ -n "$rel" ] || continue
            f="$REJECTS_DIR/$rel"
            if [ -f "$f" ]; then
              sha=$(sha256sum "$f" | awk '{print $1}')
              printf '%s\t%s\n' "$sha" "$rel" >> "$TMP"
            else
              printf '%s\t%s\n' "MISSING" "$rel" >> "$TMP"
            fi
          done < "$REJECTS_DIR/index.sorted.txt"
  
          sort "$TMP" | awk -F'\t' '
            $1=="MISSING" { missing[++mc]=$2; next }
            {
              sha=$1; rel=$2
              if (sha!=prev) {
                if (prev!="") print ""
                print "### " sha
              }
              print "- `" rel "`"
              prev=sha
            }
            END {
              if (mc>0) {
                print ""
                print "### Missing files"
                for (i=1; i<=mc; i++) print "- `" missing[i] "`"
              }
            }
          ' >> "$OUT"
  
          rm -f "$TMP"
  
          # cat "$OUT" >> "$GITHUB_STEP_SUMMARY"
  
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.FILE_NAME }}-AnyKernel3
          path: ./AnyKernel3/**
          if-no-files-found: ignore
          compression-level: 9
  
      - name: Upload Misc Files
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.FILE_NAME }}-Misc
          path: |
            ${{ env.FILE_NAME }}-*
            ${{ env.KERNEL_ROOT }}/KernelSU-Next/**
            patch-rejects/**
            build_summary.md
          if-no-files-found: ignore
          compression-level: 9
  
